# Frontend (React + Tailwind)

Basic dashboard for calling `POST /optimize` and showing:

- Optimization summary
- Target vs predicted comparison
- Best times and best masses

## Run

```bash
cd frontend
npm install
npm run dev
```

Default backend URL in UI: `http://localhost:8000`
